<UserControl x:Class="PdfConverter.Views.DropOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="900">

    <Border Background="{DynamicResource DropOverlayBackgroundBrush}"
            IsHitTestVisible="False">
        <Border Margin="24"
                BorderBrush="{DynamicResource DropOverlayBorderBrush}"
                BorderThickness="2"
                CornerRadius="18"
                Background="Transparent">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <Path Width="64"
                      Height="64"
                      Margin="0,0,0,18"
                      HorizontalAlignment="Center"
                      Stretch="Uniform"
                      Fill="{DynamicResource DropOverlayForegroundBrush}"
                      Data="M12 2 L12 14 M12 14 L8 10 M12 14 L16 10 M4 16 L4 20 L20 20 L20 16" />
                <TextBlock Text="ここにファイルをドロップ"
                           FontSize="22"
                           FontWeight="SemiBold"
                           TextAlignment="Center"
                           Foreground="{DynamicResource DropOverlayForegroundBrush}" />
                <TextBlock Text="ファイルを離すと読み込みます"
                           FontSize="13"
                           Margin="0,8,0,0"
                           TextAlignment="Center"
                           Foreground="{DynamicResource DropOverlayForegroundBrush}"
                           Opacity="0.85" />
            </StackPanel>
        </Border>
    </Border>
</UserControl>
